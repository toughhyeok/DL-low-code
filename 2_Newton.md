# Newton's method
Objective function이 $(y-A\theta)^{T}(y-A\theta)$ 일 때,

Grandient descent는 $\theta_{k+1}=\theta_{k}+A^{T}(y-A\theta_{k})\times\alpha$ 하는 방식이었다!

Newton's method는 해를 구하는 방법이다.

(작성중...)
